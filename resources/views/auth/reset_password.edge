@layout('layouts/logo')
@set('title', 'Réinitialiser son mot de passe')

@section('content')
  <form class="flex flex-col gap-2" action="{{ route('AuthController.sendResetPassword') }}" method="POST" up-target="[page-body]">
    {{ csrfField() }}

    @form.group({name: 'email' })
      @!form.label({ title: 'Adresse électronique' })
      @!form.input({ type: 'email', placeholder: 'example@site.fr'})
    @end

    <div class="mt-4 flex flex-row justify-center">
      @elements.button({
        fill: true,
        variant: 'auth'
      })
        @slot('main', className)
          <button class="{{ className }} w-full" type="submit">Envoyer le courriel de réinitialisation</button>
        @end
      @end
    </div>
  </form>
@end
