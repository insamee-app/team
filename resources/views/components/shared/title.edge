<div class="flex flex-row justify-between items-center mt-2 mb-4">
  <h1 class="text-2xl md:text-3xl lg:text-4xl font-bold">{{{ await $slots.main() }}}</h1>

  @if($slots.actions)
    {{{ await $slots.actions() }}}
  @end
</div>
