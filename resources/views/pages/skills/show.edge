@layout('layouts/app')
@set('title', skill.name)

@section('content')
  <h2 class="text-mee-primary-base text-lg font-semibold">
    <a href="{{ route('SkillsController.index') }}">
      Compétences
    </a>
  </h2>

  <div class="flex flex-row justify-between items-center mt-2">
    <h1 class="text-2xl font-bold">{{ skill.name }}</h1>
    @can('SkillPolicy.udpate')
      <a href="{{ route('SkillsController.edit', { id: skill.id}) }}" class="px-2 py-1 text-white bg-mee-primary-base shadow-sm rounded focus-within:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-mee-primary-base">Modifier</a>
    @end
  </div>

  <section class="flex flex-col mt-8">
    <h2 class="font-bold text-xl">
      Mee associés
    </h2>

    <div class="grid grid-cols-1 gap-8 mt-4">
      @each(user in users)
        <a href="{{ route('ProfilesController.show', { id: user.id }) }}">
        @!mee.card({
          user: user
        })
        </a>
      @end
    </div>

    @!shared.pagination({
      data: users,
      variant: 'mee',
      classes: 'mt-8',
    })
  </section>
@end
