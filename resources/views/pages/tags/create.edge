@layout('layouts/app')
@set('title', 'Créer un tag')

@section('content')
  <section>

    <h2 class="text-associations-primary-base text-lg font-semibold">
      <a href="{{ route('TagsController.index') }}">
        Tags
      </a>
    </h2>

    <h1 class="text-2xl font-bold mt-2">Créer</h1>

    <form action="{{ route('TagsController.store') }}" method="POST" class="mt-8">
      {{ csrfField() }}

      @form.group({ name: 'name' })
        @!form.label({ title: 'Nom' })
        @!form.input({ type: 'text', placeholder: 'Saisir le nom', classes: '!border-associations-primary-base focus:!ring-associations-primary-base focus-within:!outline-none' })
      @end

      <div class="flex flex-row justify-between mt-8">
        @elements.button({ type: 'reset', classes: 'text-black border border-associations-primary-base hover:border-slate-600 focus:!ring-associations-primary-base' })
          Annuler
        @end

        @elements.button({ type: 'submit', classes: 'text-white bg-associations-primary-base hover:bg-slate-600 focus:!ring-associations-primary-base' })
          Enregistrer
        @end
      </div>
    </form>
  </section>
@end
